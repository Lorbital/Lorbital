# 阿里云轻量服务器建站与备案流程（第一次建站）

面向：**国内访问、需 ICP 备案、纯静态站（本项目）+ 日后可能在站内收集访问数据（如各模型访问量）**。  
部署形态：**阿里云轻量应用服务器**（大陆地域）+ **域名** + **备案** + **Nginx** + **HTTPS**。

> 本项目为纯静态站，网站根目录为含 `index.html` 的目录；若工作区为内层 `Lorbital/Lorbital`，根目录在上一级 `Lorbital/`，详见 `AGENT_RUN.md`。

---

## 总览

1. 准备材料与账号  
2. 买域名并完成域名实名  
3. 买大陆轻量服务器（为备案提供接入）  
4. 走 ICP 备案（主体 + 网站信息）  
5. 管局审核通过 → 取得备案号  
6. 服务器环境（系统、Nginx、防火墙）  
7. 上传网站、域名解析、HTTPS  
8. （可选）日后增加埋点 API  

**说明**：总耗时里「只能等」的主要是**管局审核**；你自己能控制的多为若干次集中操作（每个阶段若干小时）。

---

## 第一阶段：账号与材料准备

### 做什么

- 注册/登录阿里云，完成**账号实名**（个人或企业）。  
- 准备备案材料（**个人**示例；企业另加营业执照等）：  
  - 身份证正反面清晰照片  
  - 手机号、邮箱  
  - 按页面要求填写应急联系方式等  
  - 网站名称、网站内容说明需符合实际、避免违规表述（如个人作品展示、技术介绍等，以真实内容为准）。  
- 使用浏览器访问阿里云控制台即可完成后续步骤。

### 工时（你自己）

- 熟悉要求、整理材料：**约 0.5～1 小时**  
- 若为企业主体：**约 1～2 小时**（多证件、授权书等）

### 等待

- 账号实名：**约几分钟～1 个工作日**

---

## 第二阶段：注册域名 + 域名实名

### 做什么

- 在阿里云万网选购域名并完成付款。  
- 完成**域名实名认证**（国内注册商强制要求）。  
- 不同后缀（如 `.com` / `.cn`）规则略有差异，以页面提示为准。

### 工时（你自己）

- 选名、下单、提交实名：**约 0.5～1 小时**

### 等待

- 域名实名：**约几小时～3 个工作日**

---

## 第三阶段：购买轻量应用服务器（大陆地域）

### 做什么

- 地域必须选**中国大陆**（如上海、杭州、北京等），否则无法用于**大陆网站备案**。  
- 配置：最低档通常可满足纯静态站 + 日后小型 API；不足时可再升配。  
- 系统镜像建议 **Ubuntu 22.04** 或 **Alibaba Cloud Linux**，便于安装 Nginx。  
- 记录：**公网 IP**、SSH 端口、root 或自定义用户及密码/密钥。

### 工时（你自己）

- 选购、下单、熟悉控制台：**约 0.5～1 小时**

### 等待

- 开通：**约几分钟**

---

## 第四阶段：ICP 备案

### 做什么

1. 在阿里云备案入口按向导填写**主体信息**。  
2. 填写**网站信息**（域名、网站名、服务内容、语言等）。  
3. **上传资料**，完成人脸识别或 App 核验等（以当时流程为准）。  
4. **阿里云初审**（可能电话核实，如实回答）。  
5. 提交**管局审核**。  
6. 通过后取得**备案号**，并按法规在网站底部展示（格式以阿里云说明为准）。

### 注意

- 备案期间网站如何展示、解析如何指向，**以阿里云当时规则为准**，勿擅自违规对外提供「已备案」形态的服务。  
- 若被打回，按意见修改后重新提交。

### 工时（你自己）

- 首次完整填报与沟通：**约 2～4 小时**  
- 每次修改补材料：**约 0.5～1 小时**

### 等待

- 阿里云初审：**约 0.5～2 个工作日**  
- 管局审核：**约 7～20 个工作日**（各省差异较大，仅供参考）

---

## 第五阶段：服务器基础环境

### 做什么

1. 使用 **SSH** 登录服务器（Windows 可用 PowerShell、`ssh root@公网IP`，或网页终端）。  
2. （可选）系统更新、时区设置。  
3. 安装 **Nginx**，用于 80/443 与静态资源。  
4. **防火墙**：在轻量控制台放行 **80、443**；若系统内启用 `firewalld`/`ufw`，需与之一致。  
5. 建立网站目录（如 `/var/www/站点名`），与本地**网站根**（含 `index.html` 的那一层）对应。  
6. 配置 Nginx `server`：`server_name`、 `root`、默认 `index.html`。

### 工时（你自己）

- 第一次完整搭建：**约 3～6 小时**  
- 若已熟悉 Linux：**约 1～2 小时**

### 等待

- 无

---

## 第六阶段：上传网站文件

### 做什么

- 使用 **SFTP**（如 FileZilla、WinSCP）或 `scp` 将静态文件上传到服务器网站目录。  
- 确保线上目录结构正确：**根目录下直接可见 `index.html`**，且相对路径资源（JS/CSS/模型等）与本地一致，避免多套一层目录导致 404。

### 工时（你自己）

- 首次上传与抽查各页面：**约 1～2 小时**

---

## 第七阶段：域名解析 + HTTPS

### 做什么

1. 在阿里云 **DNS 解析** 中为域名添加 **A 记录**（`@` 及需要的 `www` 等）指向轻量**公网 IP**。  
2. 等待解析生效。  
3. **HTTPS**：在阿里云申请免费 DV 证书，或使用 Let’s Encrypt（certbot）等；在 Nginx 中配置 `listen 443 ssl`，建议将 HTTP 重定向到 HTTPS。

### 工时（你自己）

- 解析 + 证书 + Nginx 配置与验证：**约 2～4 小时**（首次）

### 等待

- DNS 传播：**多数在数分钟～24 小时内生效**

---

## 第八阶段：上线检查与备案号展示

### 做什么

- 不同网络环境（如手机流量与家庭宽带）访问主要页面。  
- 在页脚添加**备案号及规定链接**。  
- 检查混合内容、死链、大资源加载等。

### 工时（你自己）

- **约 1～2 小时**

---

## 第九阶段（日后）：访问统计 / 埋点

### 做什么（概要）

- 在服务器上增加小型后端（如 Node/Python），提供例如 `POST /api/track`，记录 `modelId` 等字段。  
- 前端通过 `fetch` 上报；数据可先写入 **SQLite** 或日志文件，再按需统计。  
- 遵守《个人信息保护法》等，建议提供**隐私说明**，采集字段最小化、尽量匿名化。

### 工时（你自己，首次）

- 简单落库/日志、无管理后台：**约 4～8 小时**  
- 若增加简单统计展示页：**约再加 4～10 小时**

---

## 工时汇总表

| 阶段 | 主动投入（参考） | 主要等待 |
|------|------------------|----------|
| 账号实名 + 材料 | 0.5～1 h（企业 +1～2 h） | 分钟～1 工作日 |
| 域名注册 + 实名 | 0.5～1 h | 几小时～3 工作日 |
| 购买轻量 | 0.5～1 h | 约几分钟 |
| ICP 备案填报与沟通 | 2～4 h（每次打回约 +0.5～1 h） | 初审 0.5～2 工作日；管局常 7～20 工作日 |
| 服务器 Nginx 等 | 3～6 h（熟手 1～2 h） | — |
| 上传站点 | 1～2 h | — |
| 解析 + HTTPS | 2～4 h | DNS 多数较快 |
| 上线检查 + 备案号 | 1～2 h | — |
| （可选）简单埋点 API | 4～8 h | — |

**经验范围**：从买域名到「备案通过 + HTTPS 正式对外」，**累计动手约 12～22 小时**（第一次、偏保守），**日历时间常 3～5 周以上**（视管局而定，慢时可能更长）。

---

## 建议顺序（减少返工）

1. 账号实名 → 域名与域名实名 → 购买**大陆**轻量。  
2. **尽早提交备案**；等待期间可学习 Nginx、整理待上传文件。  
3. **管局通过**后，将正式域名与 HTTPS 配齐并对外使用（备案期内解析要求以阿里云为准）。  
4. 上线后再迭代埋点等能力。

---

## 说明

- 阿里云控制台界面与备案细则可能更新，**以官网与工单说明为准**。  
- 本文档仅作流程与工时规划参考，不构成法律或合规意见；经营类业务可能涉及额外资质，需自行核实。
