# 阿里云轻量服务器建站与备案流程（第一次建站）

面向：**国内访问、需 ICP 备案、纯静态站（本项目）+ 日后可能在站内收集访问数据（如各模型访问量）**。  
部署形态：**阿里云轻量应用服务器**（大陆地域）+ **域名** + **备案** + **Nginx** + **HTTPS**。

> 本项目为纯静态站，**网站根目录**为含 `index.html` 的那一层。若工作区是内层 `Lorbital/Lorbital`，则根目录在上一级 `Lorbital/`，与本地预览一致，见 `AGENT_RUN.md`。

---

## 一、本仓库与线上标识（随控制台更新）

以下用于你自己对照控制台，**IP、实例名等若重建实例会变化，以阿里云控制台为准**。

| 项 | 当前记录（供对照） |
|----|--------------------|
| 主域名 | `lorbital.com`（解析、证书、Nginx 的 `server_name` 均会用到） |
| 轻量实例名 | `Ubuntu-psyn` |
| 系统镜像 | **Ubuntu 24.04**（与文档里原「22.04」均为常见选择，以实例为准） |
| 规格 | 2 vCPU / **0.5 GiB 内存** / 20 GiB 盘；到期日以控制台显示为准（如 2026-07-04 前后） |
| 公网 IP | **`118.31.123.102`**（用于 SSH、SFTP、DNS 的 A 记录；**若之后换实例或变 IP 请改本表**） |
| 私网 IP | 仅同 VPC/内网互通场景使用，**公网访问与解析用公网 IP** |

**与截图相关的操作提醒**

- 实例处于 **已停止** 时，**无法** SSH、上传文件或对外提供网站；部署前请在控制台 **启动** 实例。
- **0.5 GiB 内存**对纯 Nginx+静态站足够；若日后在同一台机跑**数据库、Python/Node 后端、队列**等，建议评估**升配**，否则易 OOM 或极慢。

---

## 二、总览（你现在的位置：备案已走完）

1. 准备材料与账号  
2. 买域名并完成域名实名  
3. 买大陆轻量服务器（为备案提供接入）  
4. 走 ICP 备案（主体 + 网站信息）  
5. **管局审核通过 → 取得备案号**（你已完成）  
6. **服务器环境**（SSH、Nginx、防火墙只开必要端口）  
7. **整理「线上包」+ 上传**（与开发仓库分隔，见下文「开发与上线分隔」）  
8. **域名解析 + HTTPS**  
9. **上线检查、页脚备案号、再收紧安全策略**（见「备案后的安全与端口」）  
10. （可选）埋点、后端 API 等，与 Nginx 反代、独立目录配合  

**说明**：总耗时里「只能等」的主要是**管局审核**；备案通过后，剩余多为可集中完成的操作。

---

## 三、当前推荐顺序（备案已通过）

1. **启动**轻量实例，确认**公网 IP**未变。  
2. 用 **SSH**（或控制台「远程连接」）登录，完成 **Nginx、站点目录、443/80**，见第五阶段。  
3. 在**轻量防火墙**中只放行必要端口，见「备案后的安全与端口」。  
4. 在本地按「开发与上线分隔」**打包要上传的目录**（或维护排除清单），**SFTP/scp** 上传，见第六阶段。  
5. 在 **DNS** 为 `lorbital.com`（及 `www` 等）做 **A 记录** 指向公网 IP，配置 **HTTPS**。  
6. 全站可访问后，在页脚加**备案号及链接**；再复查防火墙与 SSH 策略。  

---

## 第一阶段：账号与材料准备

### 做什么

- 注册/登录阿里云，完成**账号实名**（个人或企业）。  
- 准备备案材料（**个人**示例；企业另加营业执照等）：  
  - 身份证正反面清晰照片  
  - 手机号、邮箱  
  - 按页面要求填写应急联系方式等  
  - 网站名称、网站内容说明需符合实际、避免违规表述（如个人作品展示、技术介绍等，以真实内容为准）。  
- 使用浏览器访问阿里云控制台即可完成后续步骤。

### 工时（你自己）

- 熟悉要求、整理材料：**约 0.5～1 小时**  
- 若为企业主体：**约 1～2 小时**（多证件、授权书等）

### 等待

- 账号实名：**约几分钟～1 个工作日**

---

## 第二阶段：注册域名 + 域名实名

### 做什么

- 在阿里云万网选购域名并完成付款（**本项目主域：`lorbital.com`**）。  
- 完成**域名实名认证**（国内注册商强制要求）。  
- 不同后缀（如 `.com` / `.cn`）规则略有差异，以页面提示为准。

### 工时（你自己）

- 选名、下单、提交实名：**约 0.5～1 小时**

### 等待

- 域名实名：**约几小时～3 个工作日**

---

## 第三阶段：购买轻量应用服务器（大陆地域）

### 做什么

- 地域必须选**中国大陆**（如上海、杭州、北京等），否则无法用于**大陆网站备案**。  
- 配置：纯静态站最低档可起步；**若计划同机跑后端+数据库，建议从更大内存起评估**。  
- 系统镜像：当前实例为 **Ubuntu 24.04**；**Alibaba Cloud Linux** 亦常见。  
- 记录：**公网 IP**、SSH 端口（默认 22）、root 或自定义用户及**密码/密钥**；密钥对可在控制台「密钥」中管理。  

### 工时（你自己）

- 选购、下单、熟悉控制台：**约 0.5～1 小时**

### 等待

- 开通：**约几分钟**

---

## 第四阶段：ICP 备案

### 做什么

1. 在阿里云备案入口按向导填写**主体信息**。  
2. 填写**网站信息**（域名、网站名、服务内容、语言等）。  
3. **上传资料**，完成人脸识别或 App 核验等（以当时流程为准）。  
4. **阿里云初审**（可能电话核实，如实回答）。  
5. 提交**管局审核**。  
6. 通过后取得**备案号**，并按法规在网站底部展示（格式以阿里云说明为准）。  

### 注意

- 备案期间网站如何展示、解析如何指向，**以阿里云当时规则为准**，勿擅自违规对外提供「已备案」形态的服务。  
- 若被打回，按意见修改后重新提交。

### 工时（你自己）

- 首次完整填报与沟通：**约 2～4 小时**  
- 每次修改补材料：**约 0.5～1 小时**

### 等待

- 阿里云初审：**约 0.5～2 个工作日**  
- 管局审核：**约 7～20 个工作日**（各省差异较大，仅供参考）

---

## 第五阶段：服务器基础环境

### 在阿里云官网操作，还是在本地操作？

是**两边都会用到**，分工不同：

| 在哪里 | 做什么 |
|--------|--------|
| **阿里云官网（轻量控制台）** | 启动/查看实例、看公网 IP、**防火墙**里放行 80、443、22 等；可选点「**远程连接**」在浏览器里打开**网页版终端**（不装 SSH 也能进系统）。 |
| **你的电脑（本地）** | 用 **PowerShell** 或 **WinSCP / FileZilla** 等，通过**公网 IP** `118.31.123.102` 连上服务器；装 Nginx、改配置、建目录等**命令，都是在「已经登录进去的那台 Ubuntu」里执行**，只是**登录方式**可以选：本地 `ssh` 或 控制台的远程连接。 |

一句话：**改防火墙、开机关机在官网；装 Nginx、写配置在服务器里完成——你从本地或网页终端连进去，效果一样。**

### 做什么

1. 确认实例为**运行中**，使用 **SSH** 登录（Windows 可用 PowerShell：`ssh 用户名@118.31.123.102`，或控制台**远程连接/网页终端**）。  
2. （可选）`apt update`、时区 `timedatectl` 等。  
3. 安装 **Nginx**：对外提供 80/443 与静态资源。  
4. **防火墙（两层一致）**  
   - 在**轻量控制台 — 防火墙**中放行 **80、443**；用于维护时再开 **22**（见安全章节）。  
   - 系统内若启用 `ufw`，规则需与控制台一致，避免「一头放行一头拦截」。  
5. 建立网站目录（例如 `/var/www/lorbital`），与「网站根」一致（见「开发与上线分隔」）。  
6. 配置 Nginx `server`：`server_name lorbital.com www.lorbital.com;`、`root`、 `index index.html;`，必要时重定向、静态缓存等。  

### 工时（你自己）

- 第一次完整搭建：**约 3～6 小时**  
- 若已熟悉 Linux：**约 1～2 小时**

### 等待

- 无

---

## 第六阶段：上传网站文件

### 做什么

- 使用 **SFTP**（如 FileZilla、WinSCP）或 `scp`/`rsync`，将**线上包**传到服务器上一步的 `root` 目录。  
- **根目录下须直接可见 `index.html`**，且 `src/`、`css/`、`public/`、`models/model++/` 等相对路径与本地一致，避免多嵌套一层导致 404。  
- 上传用户通常需要对该目录的写权限（`chown` 给 `www-data` 或 `scp` 用户，按你的 Nginx 运行用户定）。  

### 工时（你自己）

- 首次上传与抽查各页面：**约 1～2 小时**

---

## 第七阶段：域名解析 + HTTPS

### 做什么

1. 在阿里云 **DNS 解析** 中，为 `lorbital.com` 添加 **A 记录**（`@`）指向轻量**公网 IP**；需要时增加 `www` 的 A 或 CNAME。  
2. 等待解析生效。  
3. **HTTPS**：阿里云免费 DV 证书、或 Let’s Encrypt（certbot）等；Nginx 中 `listen 443 ssl`，并建议将 HTTP 重定向到 HTTPS。  
4. 证书中域名需与浏览器访问的域名一致（含 `www` 与裸域是否都覆盖）。  

### 工时（你自己）

- 解析 + 证书 + Nginx 配置与验证：**约 2～4 小时**（首次）

### 等待

- DNS 传播：**多数在数分钟～24 小时内生效**

---

## 第八阶段：上线检查与备案号展示

### 做什么

- 手机流量、家庭宽带等多网络访问 `https://lorbital.com` 下主要入口（`/explorer.html` 等）。  
- 在页脚添加**备案号及规定链接**（样式以当时法规与阿里云说明为准）。  
- 检查混合内容、大资源、PLY 路径是否正确。  

### 工时（你自己）

- **约 1～2 小时**

---

## 第九阶段（日后）：访问统计 / 埋点 / 后端

### 做什么（概要）

- 在服务器上为 API 单设目录或独立用户，**Nginx 反代**到本机 `127.0.0.1:端口` 的 Node/Python 服务，**不要**把数据库端口暴露到公网。  
- 埋点可 `POST /api/track` 等；数据用 SQLite/日志文件等，**遵守《个人信息保护法》**，站内需有**隐私政策**、字段最小化。  
- 内存仅 **0.5 GiB** 时，**数据库与重计算慎放同机**，或升配/拆分为独立服务。  

### 工时（你自己，首次）

- 简单落库/日志、无管理后台：**约 4～8 小时**  
- 若增加简单统计展示页：**约再加 4～10 小时**

---

## 备案后的安全与端口（你朋友提的「关端口、防攻击」）

备案通过后，**域名、主体名称、网站名称等**会在备案查询侧公开，这是法规要求，与「开哪些端口」不是同一回事，但**公网能打的入口越少、配置越严，被扫、被撞库的概率越低**。

建议按优先级做：

1. **轻量「防火墙」模板**中：  
   - **必须**：`80`、`443`（若只用 HTTPS，仍常保留 80 做跳转）。  
   - **维护时**：`22`（SSH/SFTP 与 SSH 同端口），**不维护时可临时关闭 22**（关前务必确认有**控制台 VNC/救援**能进去，或改用**仅自己 IP 可见**的放通策略，若产品支持）。  
   - **不要**长期开放 `3306`、`6379`、`27017`、`21` 等，除非清楚用途并配强鉴权/专网。  

2. **SSH**：强密码或**密钥登录**；禁用空密码、限制 `root` 密码直登（若用密钥则更安全）、可考虑 `fail2ban` 防暴力破解。  

3. **仅暴露 Nginx**：将来有后端，让应用监听 `127.0.0.1`，经 Nginx 反代，避免应用端口直接对公网。  

4. **Web 上不要出现**：服务器管理密码、数据库密码、`.env` 备份、**完整内网结构说明**、未脱敏的运维文档。  

5. 定期**系统补丁**、关注阿里云安全公告。  

---

## 开发与上线分隔（本仓库怎么上传、什么不要上传）

目标：**开发机**保留完整历史、规划、生成脚本和大数据；**服务器**只放**运行站点所必需、且可公开**的那一层，减少泄露面与误传大文件。  

### 网站根在仓库里的位置（与 `AGENT_RUN.md` 一致）

- 含 `index.html`、`explorer.html` 的目录 = **网站根**（在多数克隆布局里为**外层** `Lorbital/`，内层是 `Lorbital/Lorbital` 的代码与资源时的上一层）。  
- 线上 Nginx 的 `root` 应指向与本地 `--directory` 时**相同那一层**的内容。  

### 建议上传到服务器的（运行静态站需要）

- `index.html`、`*.html` 等入口与页面  
- `css/`、`src/`、`public/`（及站内引用到的静态资源）  
- 浏览器要加载的 **`models/model++/` 下 PLY 等**（体积大，按需分批上传与核对路径）  
- 若根目录有 `favicon`、 `robots.txt` 等，一并同步  

### 一般不要整站同步到公网机（用 SFTP/打包时排除或单独同步）

- **`.git/`**：除非你用服务器做 CI/CD，通常不必上传，避免历史与旧密钥泄露。  
- **`models/model.py`、各类 Python 计算脚本、`.py` 生成线**：不用于线上渲染，**不必上传**；需要时在「日后后端」中单独放目录与鉴权。  
- **`docs/`、内部 `README` 中研发细节**、**`.cursor/`、`.tmp_*/`、临时文档/中间稿**  
- 本地调试日志、**环境变量文件**、**密钥/证书私钥**（除 Nginx/正式证书在服务器本机装）  
- 与站点无关的**大中间数据**、未脱敏的**个人或商务信息**  

### 操作习惯建议

- 维护一个**上传清单**或脚本（如 `rsync` 的 `--include`/`--exclude`），每次发布只覆盖「网站根 + 已变更的 model++ 子集」，比「整个仓库」更安全、更省流量。  
- 测试环境可用**子域名** + 强鉴权，与正式 `lorbital.com` 分离；后端上线后再加**独立子域**和证书。  

---

## 工时汇总表

| 阶段 | 主动投入（参考） | 主要等待 |
|------|------------------|----------|
| 账号实名 + 材料 | 0.5～1 h（企业 +1～2 h） | 分钟～1 工作日 |
| 域名注册 + 实名 | 0.5～1 h | 几小时～3 工作日 |
| 购买轻量 | 0.5～1 h | 约几分钟 |
| ICP 备案填报与沟通 | 2～4 h（每次打回约 +0.5～1 h） | 初审 0.5～2 工作日；管局常 7～20 工作日 |
| 服务器 Nginx 等 | 3～6 h（熟手 1～2 h） | — |
| 整理线上包 + 上传站点 | 1～2 h | — |
| 解析 + HTTPS | 2～4 h | DNS 多数较快 |
| 上线检查 + 备案号 + 安全复查 | 1～2 h | — |
| （可选）简单埋点 API | 4～8 h | — |

**经验范围**：从买域名到「备案通过 + HTTPS 正式对外」，**累计动手约 12～22 小时**（第一次、偏保守），**日历时间常 3～5 周以上**（视管局而定，慢时可能更长）。  

---

## 建议顺序（减少返工）

1. 账号实名 → 域名与域名实名 → 购买**大陆**轻量。  
2. **尽早提交备案**；等待期间可学习 Nginx、整理**线上包**与排除清单。  
3. **管局通过**后：环境 → 上传 → 解析与 HTTPS → 备案号与**防火墙/SSH 收紧**。  
4. 需要更强算力或后端时，再**升配**或拆分服务。  

---

## 说明

- 阿里云控制台界面与备案细则可能更新，**以官网与工单说明为准**。  
- 本文档仅作流程、工时与工程习惯参考，**不构成法律或合规意见**；经营类业务可能涉及额外资质，需自行核实。  
- 公网 IP、规格以控制台为准；**本文「当前记录」表在变更实例后请自行更新，避免对不上 SSH 与解析。**
